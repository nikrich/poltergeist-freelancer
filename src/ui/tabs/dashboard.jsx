// Dashboard tab — placeholder. Full stats/needs-attention/recent-activity
// layout lands in a later task.

import { Panel } from '../kit.jsx';

export function DashboardTab({ s }) {
  return (
    <Panel title="dashboard" s={s}>
      <div style={{ fontSize: 12, color: s.ink2 }}>coming in the next task.</div>
    </Panel>
  );
}
