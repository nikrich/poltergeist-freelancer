// Clients tab — placeholder. Client roster + detail view lands in a later task.

import { Panel } from '../kit.jsx';

export function ClientsTab({ s }) {
  return (
    <Panel title="clients" s={s}>
      <div style={{ fontSize: 12, color: s.ink2 }}>coming in the next task.</div>
    </Panel>
  );
}
